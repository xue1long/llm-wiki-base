# R11 — 锁定依赖清单（2026-08-18 实测安装版本）
#
# 用法（离线可复现安装，绕过代理）:
#   pip install -r requirements.lock
#   pip install -e .            # 或 pip install -r requirements.lock -e .
# 可选 embedding profile（本地语义检索）:
#   pip install -e ".[embedding]"
#
# 生成方式: 从 pyproject.toml 声明 + 本机 pip list 实测版本交叉核对。
# 升级依赖后运行: pip list --format=freeze | 过滤核心包 → 更新此文件。
# 本仓库不承诺跨版本兼容, 锁文件是唯一可复现安装入口。

# ── 运行时核心 ──
fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.12.5
pydantic-settings==2.13.1
httpx==0.28.1
platformdirs==4.11.0
PyYAML==6.0.3
lancedb==0.34.0
pypdf==6.14.2
pymupdf==1.28.2
python-docx==1.2.0
openpyxl==3.1.5
mcp==1.28.1

# ── dev ──
pytest==9.0.3
pytest-asyncio==1.3.0
ruff==0.16.1
mypy==2.3.0

# ── 可选: 本地 embedding (R11 profile) ──
# sentence-transformers>=2.2.0   (通过 pip install -e ".[embedding]" 安装)
