# 个人软件规范化开发标准SOP｜单人跨设备开发规范

## 基础约定
1. 开发模式：单人独立开发，多台设备交替开发；所有规范永久生效，不受设备切换影响。
2. 架构底线：功能模块**内部高度内聚、模块之间低耦合**；模块仅通过对外暴露API通信，禁止直接导入其他模块内部私有实现。
3. 强制遵循流水线，禁止跳步开发：构思方案 → 制定计划 → 定义契约 → 实现代码 → 测试验证 → 代码自查评审 → 提交收尾。
4. 默认启用TDD思想：先定义接口、先编写测试用例，再实现业务逻辑。
5. 任何改动不得直接上手写代码；需求模糊时先暂停编码，完成调研与方案记录。

## 一、需求与方案阶段（brainstorming）
### 准入规则
收到新功能、BUG修复、重构需求后，第一步执行本阶段，不直接编码。
输出文件统一存放路径：`docs/plans/`
1. 梳理需求：明确功能目标、输入输出、边界场景、不做的功能（划定范围，防止无限蔓延）
2. 架构影响评估：判断改动涉及哪些模块，会不会破坏现有模块边界，是否产生循环依赖
3. 可选方案对比，记录风险点、依赖限制、兼容性问题
4. 确认模块交互方式：同步API调用 / 事件总线 / 消息通知；优先事件总线消除双向依赖
5. 产出《功能方案.md》，内容模板：
    - 需求描述
    - 影响模块清单
    - 模块新增/修改对外契约
    - 异常场景处理策略
    - 自测验收标准
> 禁止：脑子里空想方案，无文档直接编码。

## 二、任务规划阶段（writing-plans）
基于方案文档拆解最小任务清单
1. 任务粒度：单个任务只修改**一个模块**
2. 任务顺序自上而下，依赖前置任务先完成
3. 明确每个任务产出、验证方式
4. 如果任务较大，切分为多个子任务，分批迭代实现
> 规则：没有任务清单，不启动编码工作。

## 三、架构 & 模块强制约束（永久生效）
### 模块设计规范
1. 每个独立功能模块为独立目录
2. 目录标准范式
```
modules/xxx-module/
├── api.ts       # ✅唯一对外入口，所有外部模块只能导入此文件
├── types.ts     # 对外导出类型、接口契约
├── service/     # 内部业务逻辑【私有，外部禁止导入】
├── model/       # 内部数据模型【私有】
├── utils/       # 模块内部工具【私有】
└── tests/       # 模块单元测试
```
3. 导入禁令
❌ 禁止跨模块导入 service/ model/ utils 内部文件
✅ 允许跨模块导入目标模块 api.ts + types.ts
4. 循环依赖处理原则
- 优先抽离公共类型/通用逻辑至 `core/` 公共层
- 同步双向依赖改为事件驱动模式
- 无法解耦则新增协调中间模块

### 公共目录约定
- `core/`：内核、事件总线、DI容器、基础工具、通用抽象接口
- `docs/`：方案文档、架构图、接口说明、开发计划
- `scripts/`：构建、检查、部署自动化脚本
- `config/`：多环境配置文件

## 四、编码实现阶段（executing-plans）
### 1. 契约先行
新增功能优先定义对外接口、类型声明，确定入参、返回值、错误结构，再写实现代码。

### 2. 代码风格统一
- 严格遵循项目内已有的 ESLint / Prettier / 格式化规则
- 命名语义化；禁止无意义缩写、魔法数字
- 复杂逻辑增加注释，说明意图，不重复复述代码做什么

### 3. 分层规范
业务逻辑与接口层、数据访问层分离；接口层只做参数校验、转发，不堆砌业务代码。

### 4. 新增依赖约束
新增第三方包前评估：体积、维护状态、许可证、潜在安全风险；记录到依赖说明文档。

## 五、测试与验证阶段（verification-before-completion + test-driven-development）
1. 单元测试：模块独立逻辑编写单元测试，可以单独运行，不依赖完整项目启动
2. 集成测试：模块之间互相调用场景验证
3. 人工自测：覆盖正常流程、异常输入、边界情况、错误回滚场景
4. 所有新增代码必须配套对应测试；重构代码保证原有测试全部通过
> 规则：自测不通过，不允许提交代码。

## 六、自查评审阶段（requesting-code-review / receiving-code-review，单人自评审）
单人开发无同事评审，执行**自我代码评审清单**，逐条检查：
1. 是否违反模块导入规范，存在跨模块调用私有代码
2. 是否存在循环依赖风险
3. 对外接口是否最小化暴露，没有泄露内部实体
4. 异常捕获完备，错误信息规范
5. 是否存在冗余代码、注释掉的废弃代码
6. 测试用例是否覆盖核心分支
7. 文档是否同步更新（接口变更同步更新文档）
自查发现问题，立即修复。

## 七、调试排障规范（systematic-debugging）
遇到BUG禁止随意试错调试：
1. 记录复现步骤、输入、预期结果、实际结果
2. 定位最小出错范围，区分：参数问题 / 逻辑问题 / 模块通信问题 / 外部依赖问题
3. 修复后补充对应的测试用例，防止再次复现
4. 疑难问题记录到 `docs/bugs/`

## 八、Git & 提交规范（跨设备协作核心）
1. 分支策略
- main：稳定可用版本，永远保持可编译运行
- feature/xxx：功能开发分支
- fix/xxx：缺陷修复分支
2. Commit 强制格式
`[类型](模块): 简短描述`
类型可选：feat / fix / refactor / docs / test / chore
示例：`[feat](user): 新增用户登录接口`
3. 提交前自动化检查：格式化、静态代码检查、单元测试
4. 切换设备前：完成当前任务，提交代码推送远程；禁止本地大量未提交代码跨设备迁移
5. 合并至main前，完整运行一次全量测试

## 九、文档维护规则
1. 方案文档、架构文档跟随代码改动同步更新，不一次性写完永久搁置
2. 所有自定义API、模块通信规则写入文档
3. 重要设计决策记录：`docs/adr/`（架构决策记录），记录当时为什么这么设计
4. 废弃接口、废弃模块标记归档，逐步清理，不长期保留垃圾代码

## 十、分支收尾（finishing-a-development-branch）
功能开发完毕准备合并前执行收尾检查：
1. 删除临时调试代码、console、测试硬编码参数
2. 清理注释掉的无效代码
3. 核对文档是否全部更新
4. 确认测试全部通过
5. 整理本次迭代技术债务，记录后续重构计划

## 十一、迭代红线（绝对禁止行为）
1. 需求不清晰直接编码
2. 跳过方案、计划环节直接写代码
3. 破坏模块边界，随意跨模块引入内部代码
4. 不写测试，仅凭人工简单测试
5. 大量未提交代码留在本地，切换设备导致代码丢失、冲突
6. main分支频繁提交未完成、无法运行的代码
7. 接口变更不更新文档，后期无法追溯
8. 一次性堆砌超大PR，一个commit完成大量不相关改动

## 十二、与AI交互指令约定
当向 Claude Code 下发开发需求时：
1. AI首先确认：本改动属于哪个模块、是否需要新增模块、模块交互方式
2. AI主动输出简易方案与任务拆分清单，等待确认后再编码
3. 改动完成自动执行自评审清单
4. 如果我提出重构，AI优先评估对现有模块耦合的影响，提出解耦方案
5. 任何涉及模块API变更，同步更新 types 类型定义与文档

---

## 本仓库适配说明（ruflo-kb）

> 上述 SOP 是通用规范。本仓库（Python 3.11+ 多智能体知识库平台）落地时做如下映射：

| SOP 术语 | 本仓库对应 |
|----------|-----------|
| `api.ts`（唯一对外入口） | 模块 `__init__.py` 的 facade re-export（如 `from src.wiki import WikiPage, WikiPaths`） |
| `types.ts`（契约） | `src/<module>/types.py` 或 `schemas.py` |
| `service/`（私有） | `src/<module>/service/` 内部实现，外部禁止导入 |
| `core/` 公共层 | `src/lib/`、`src/events/`（事件总线） |
| 事件总线交互 | `src/events/event_bus.py` + `src/pipeline/pipeline.py` 注册处理器 |
| 方案文档 | `docs/plans/` |
| 架构决策 | `docs/adr/` |
| 测试 | `tests/test_<module>.py`，运行需 `PYTHONPATH=. pytest --import-mode=importlib` |
| Commit 格式 | `type(scope): 描述`（feat/fix/refactor/docs/test/chore） |

**导入禁令在本仓库的体现：** 生产代码禁止 `from src.wiki.ensure import X` 这类旧模块路径（已移除别名）；必须走分层路径 `src.wiki.core.paths` / `src.wiki.storage.ensure` / `src.wiki.features.*`，或经 `src/wiki/__init__.py` 的 facade 重导出。

**启动 SOP 用法：**
> 严格遵循项目根目录 PROJECT_SOP.md 内所有单人开发规范，所有开发动作遵守流水线，禁止跳步。接到需求先输出方案，得到确认后再进行编码。